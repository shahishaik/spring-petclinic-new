apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pet-helm.fullname" . }}-test-connection"
  labels:
    {{- include "pet-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pet-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
