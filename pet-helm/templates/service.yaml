apiVersion: v1
kind: Service
metadata:
  name: {{ include "pet-helm.fullname" . }}
  labels:
    {{- include "pet-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pet-helm.selectorLabels" . | nindent 4 }}
